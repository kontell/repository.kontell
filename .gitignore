# The served site lives on the gh-pages branch, checked out here as a worktree.
/_site/

# Generated artifacts — now produced into the gh-pages worktree, not main.
/repository.kontell-*.zip
/index.html

# Python bytecode cache.
__pycache__/

# Cloudflare Worker local artifacts.
worker/node_modules/
worker/.wrangler/

# Local analytics report -- holds Cloudflare credentials, keep out of git.
/scripts/analytics.sh
